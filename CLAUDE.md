# wr_spellcheck 프로젝트 규약

## 문서 규칙
- **모든 문서는 300자 이내**로 작성한다. 한 문서가 길어지면 쪼개서 하위 문서로 분리한다.
- `doc/` 폴더는 문서 관계에 따라 **하위 폴더 구조**를 만들어 점진적으로 보강한다. 평면 나열을 피하고, 같은 주제는 한 폴더로 묶는다.
- 코드 작성 단계에서 **각 세부 폴더마다 workflow / test / action 등 행동 단위 작업을 모두 문서로 남긴다**. 코드는 문서에 대응한다.

## 작업 순서
- 신규 아이디어는 코드 전에 먼저 `doc/`에 문서로 정리하고 사용자 확인을 받는다.
- 한 문서 안에서 결론을 다 내리려 하지 말고, 분기마다 새 문서로 갈라 보강한다.
