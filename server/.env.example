# 서버 환경변수 예시. 실제 값은 .env (.gitignore)에 두거나 Railway 환경변수로 주입.

# API 키. 설정 시 모든 /v1/* 요청에 X-API-Key 또는 Bearer 헤더 검증.
# 미설정 시 로컬 개발 모드로 간주 — 검증 생략.
WR_API_TOKEN=replace-with-strong-secret

# Express 포트 (Railway는 자동 주입).
# PORT=3000
