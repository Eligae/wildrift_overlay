# 로드맵 — PR 단위

각 PR은 독립 빌드 + 실기 검증 가능 단위.

## MVP (v0.1.0 — 완료)
1. Gradle 셋업, 빈 `MainActivity`.
2. `SYSTEM_ALERT_WINDOW` 부여 안내 + 인텐트.
3. `OverlayService` (포그라운드/알림) + 빈 오버레이.
4. 슬롯 5 × 버튼 2 레이아웃.
5. `SlotState` + 타이머 + 시각 상태 4단계.
6. 손잡이 드래그/더블탭 + 직렬화.

## v0.2.0 (주력 — 자동 채팅 감지)
7. Accessibility 노드 dump → A/B 기술 결정.
8. 실제 감지 + 슬롯 자동 카운트다운.

## 첫 출시 (v0.2.0+)
- GitHub Actions 빌드/서명 + Release.
- GitHub Pages 소개.

## v1.x 이후
- 추천 / 조합 / iOS / 궁극기 재도입.
